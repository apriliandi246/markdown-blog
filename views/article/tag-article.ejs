<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <link rel="stylesheet" href="../../css/navbar-1.css">
   <link rel="stylesheet" href="../../css/home.css">
   <script defer src="../../js/collapse.js"></script>
   <title><%= tag %></title>
</head>

<body>
   <%- include('../partials/navbar-1') %>

   <div class="container">
      <% if(articles.length > 0) { %>
      <div class="collapse">
         <button class="collapse-button">Choose tag<i class="collapse-icon">+</i></button>
         <div class="collapse-content">
            <%- include('../partials/tags') %>
         </div>
      </div>

      <div class="articles">
         <% articles.forEach((article) => { %>
         <div class="article">
            <a href="/article/detail/<%= article.slug %>" class="title"><%= article.title %></a>
            <p class="published"><%= formatDate(article.createdAt) %></p>
            <div class="tag"><%= article.tag %></div>
            <hr>
         </div>
         <% }) %>
      </div>

      <% } else { %>
      <h1 class="no-article">Article with tag "<%= tag %>" doesn't exist right now 😅</h1>
      <% } %>
   </div>
</body>

</html>